def phase_time_isValid(phase_time):
    """Check if the phase time is valid / Verifica se a duração da fase é válido"""
    return phase_time > 0

def phase_isValid(phase):
    """Check if the phase is valid / Verifica se a fase é válida"""
    return phase > 0